import 'package:flutter/material.dart';

const HEADER_HEIGHT = 70.0;

const ContainerDecoration = BoxDecoration(
  gradient: LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [Color(0xFF1B1C2F), Color(0xFF2E355C)],
    stops: [0.1, 1],
  ),
);

const BLUE_COLOR = Color(0xFF465192);

const H3_STYLE = TextStyle(
  fontSize: 20,
  color: Colors.white,
);

const H6_STYLE = TextStyle(
  fontSize: 12,
  color: Colors.white,
);

const INITIALS = "AS";
